# greeting is a test
greeting=hello, this is demo

# 开发环境配置文件
#spring.profiles.active=develop

# log 
# logging.config=logback.xml

# database connection message
spring.datasource.url = jdbc:mysql://10.1.236.12:3306/mlf
spring.datasource.username = devmlf
spring.datasource.password = dev123456


# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# redis url && port
spring.redis.host=10.1.236.13
spring.redis.port=6379

#==================================================
# zk 连接地址
#==================================================
#zk.connection.path=10.200.73.32:2181,10.200.73.33:2181,10.200.73.34:2181
zk.connection.path=10.1.236.13:2181

server.port=8080
